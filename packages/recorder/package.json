{
  "name": "@vidtreo/recorder",
  "version": "0.0.0",
  "description": "Video transcoding package using mediabunny",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build:js && bun run build:types && bun scripts/flatten-types.ts && bun run build:wc",
    "build:js": "bun build ./src/index.ts --outdir ./dist --target node --format esm --packages external --sourcemap=none",
    "build:dev": "bun build ./src/index.ts --outdir ./dist --target node --format esm --packages external --sourcemap=external && bunx tsc --emitDeclarationOnly --declaration --outDir dist --declarationMap && bun scripts/flatten-types.ts --dev",
    "build:prod": "NODE_ENV=production bun build ./src/index.ts --outdir ./dist --target node --format esm --packages external --sourcemap=none --minify-whitespace --minify-identifiers --minify-syntax --drop console --drop debugger && bunx tsc --emitDeclarationOnly --declaration --outDir dist --declarationMap false && bun scripts/flatten-types.ts --prod",
    "build:types": "bunx tsc --emitDeclarationOnly --declaration --outDir dist --declarationMap false",
    "build:wc": "vite build --config vite.wc.config.ts",
    "build:wc:dev": "vite build --config vite.wc.config.ts --mode development",
    "build:all": "bun run build && bun run build:wc",
    "clean": "rm -rf dist",
    "dev": "vite --config dev/vite.config.ts",
    "dev:wc": "vite --config vite.wc.config.ts",
    "test": "bun test --concurrent",
    "test:watch": "bun test --watch --concurrent",
    "test:coverage": "bun test --coverage --concurrent"
  },
  "keywords": [
    "video",
    "transcoding",
    "mediabunny",
    "mp4"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "mediabunny": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/postcss-load-config": "^3.0.1",
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.6",
    "vite": "^5.4.11"
  },
  "publishConfig": {
    "access": "public"
  }
}
